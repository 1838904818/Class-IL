# Prospective input preparation

8 September 2026. Status: metadata prepared and 16 source arrays hash-verified locally. No derivation, preprocessing fit, embedding export or training was executed.

The original source manifest matches SHA-256 99f6de7a6cdd09b91e9bc0e167304db4d4274e753adad5138ec7803f5338a15b. All eight train.npy and eight test.npy files match the per-class SHA-256 values in prospective_sampling/REPLAYIDS_INPUTS.json. This verifies source bytes, not capture independence or absence of cross-split semantic duplicates.

The metadata adapter completed and generated increment_00.json through increment_03.json, official_test.json and ADAPTER_COMPLETE.json. Training and official-test contracts remain separate. The first invocation stopped before writing because the output parent was absent; after creating a new local parent, the second invocation returned METADATA_PREPARED.

The source uses row_identity_proxy rather than independently validated capture groups. Do not upgrade that status. The next empirical stage is measured Task-0 O/P derivation, followed by a common-cohort encoder and matched objective comparisons. No historical encoder may substitute for the new cohort while claiming matched pretraining. Institutional execution requires fresh exact-hash review and authorization.
