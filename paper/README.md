# Manuscript status

The current supervisor revision is under `checkpoint_selection_2026-09-01/`:

- `OFRA_ETG_Manuscript_v2_9_Checkpoint_Selection_2026-09-01.docx` and `.pdf`;
- `OFRA_ETG_Technical_Document_v2_9_2026-09-01.docx` and `.pdf`.

The current paper reports:

- strict five-seed prediction evidence for five independently processed
  datasets;
- the MalayaNetwork_GT seed-1 explanation analysis of the registered
  `joint_cap3000` class margin;
- a three-method attribution-sensitivity pilot;
- offline ETG governance outcomes bound to completed DICC evidence; and
- the completed seed-42 FTP-Patator O1 diagnostic: 0% pre-label unknown recall,
  followed by 82.30% post-label new-class recall and a 0.27-percentage-point
  old-class accuracy reduction; and
- the completed paired five-seed checkpoint-selection diagnostic, which is
  mixed and statistically inconclusive and therefore does not replace the
  last-epoch primary protocol.

The present single-seed explanation result is `12/17` eligible
class-by-adjacent-checkpoint transitions and must not be interpreted as a
packet, flow, sample, or production-incident rate.

The manuscript does not claim autonomous unknown discovery, automatic head
creation, online ETG control, or universal superiority. The current validated
result index is under `results/`.
