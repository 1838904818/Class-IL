# Manuscript status

The supervisor-revision manuscript is aligned with the validated evidence
available on 12 August 2026.

The current paper reports:

- four-seed descriptive FT-Transformer 512x12 results for MalayaNetwork_GT and
  NSL-KDD;
- the MalayaNetwork_GT seed-1 explanation analysis of the registered
  `joint_cap3000` class margin;
- offline ETG governance outcomes bound to completed DICC Job 389896; and
- explicit limitations for the incomplete fifth seed, remaining formal
  datasets, multi-seed explanation analysis, attribution-method robustness,
  and the absence of an online ETG-to-OFRA feedback loop.

The present single-seed explanation result is `12/17` eligible
class-by-adjacent-checkpoint transitions and must not be interpreted as a
packet, flow, sample, or production-incident rate.

The manuscript is a supervisor revision, not a final submission-ready claim of
superiority. The current validated result index is under `results/`.
