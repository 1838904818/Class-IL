# Checkpoint-policy scope

ReplayIDS OFRA results in this immutable export package are the **guarded-checkpoint secondary**, Job 426307. They are not the last-epoch primary, Job 425539. The `official/joint_cap3000` inference-arm name is shared by both policies and must not be used as a substitute for checkpoint provenance.

See [the primary comparison and scoring diagnostic](../../docs/REPLAYIDS_PRIMARY_SCORING_DIAGNOSTIC.md). The correction adds a retrospectively labelled primary comparison; it does not overwrite any JSON export or change the hashes in EXPORT_SHA256.json. Malaya entries are not changed.
